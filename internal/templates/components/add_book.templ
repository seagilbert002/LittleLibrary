 package components

import (
    "github.com/seagilbert002/LittleLibrary/internal/models"
    "github.com/seagilbert002/LittleLibrary/internal/templates"
    
    "strconv"
    )

templ AddBook(book *models.Book) {
    @templates.Layout("Book Details") {
        
    }
}
